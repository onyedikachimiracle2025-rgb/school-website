# Miraz High School Site

This repository contains simple static files for the Miraz High School site/demo. The main file in this folder is `bluetooth_content_share.html` which you can open in a browser to preview the page.

How to view locally

1. Open the folder in your file manager.
2. Double-click `bluetooth_content_share.html` or open it in your browser.

Preparing and pushing to GitHub

If you want to publish this project to GitHub, run the following from this project folder in PowerShell:

```powershell
cd "C:\Users\Heismiraz Savage\Desktop\Miraz High School Site"
git init
git add .
git commit -m "Initial commit"
# Create a repo on GitHub (replace placeholders) and push:
git remote add origin https://github.com/<your-username>/<repo-name>.git
git branch -M main
git push -u origin main
```

Replace `<your-username>` and `<repo-name>` with your GitHub username and desired repository name.

License

This project includes an MIT license by default in `LICENSE`. Replace the author name there if needed.

Security note

Before pushing, remove any secrets or credentials (files like `.env`) and large binaries (>100 MB). Use Git LFS if needed.

---
_Generated by assistant to prepare the project for sharing._
